import React, { memo } from 'react'
import Header from '../../components/Header';

function About(){
  return (
    <div>
      <Header />
      about
    </div>
  )
}


export default React.memo(About);